name: com.newrelic.nagios
description: Reports status and metrics for nagios service
protocol_version: 2
os: windows

commands:
  metrics:
    command:
      - .\bin\nri-nagios.exe
      - --metrics
    interval: 15

name: com.newrelic.nagios
description: Reports status and metrics for nagios service. Legacy file kept for compatibility.
protocol_version: 2
os: windows

commands:
  metrics:
    command:
      - .\bin\nri-nagios.exe
      - --metrics
    interval: 15
